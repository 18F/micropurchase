<%= link_to(
      t('auctions.show.status.work_in_progress.action'),
      auction_path(status.auction, auction: { status: 'pending_acceptance' }),
      method: :patch,
      class: 'usa-button usa-button-outline action-button'
) %>
