<h1>Create auction</h1>
<div class="auction-form">
  <%= simple_form_for [:admin, @auction.new_record] do |f| %>
    <%= render partial: 'form', locals: { f: f } %>
    <%= f.submit class: 'usa-button usa-button-outline' %>
  <% end %>
</div>
