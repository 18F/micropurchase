<%= link_to(
  t('statuses.bid_status_presenter.over.winner.pending_payment_confirmation.action'),
  auction_receipts_path(status.auction),
  method: :post,
  class: 'usa-button usa-button-outline action-button'
) %>
